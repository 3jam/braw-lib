#lang info

(define collection 'multi)

(define deps '("base" "scribble-lib"))

(define scribblings '(("scribblings/braw.scrbl" ())))

(define pkg-desc "raw/cooked terminal handling")

(define version "0.1")

(define pkg-authors '(jam))
